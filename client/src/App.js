import React from 'react';
import Chat from './Chat'; // Ensure the Chat component is in the same folder

const App = () => {
    return (
        <div>
            <h1>Chatbot App</h1>
            <Chat />
        </div>
    );
};

export default App;
